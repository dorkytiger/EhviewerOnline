/// 一次下载任务的结果。
///
/// 刻意**不是** `Result<DownloadOutcome>` 里的错误分支：用户主动取消不是失败，
/// 把它做成异常会逼着上层去比较错误文案才能分辨「取消了」和「网络断了」，而文案
/// 是给人看的，随时会改。
enum DownloadOutcome {
  /// 所有页都写完了。
  completed,

  /// 用户取消；已下载的页保留，下次可以从断点继续。
  cancelled,
}
